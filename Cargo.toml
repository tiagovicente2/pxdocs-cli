[package]
name = "pxdocs-cli"
version = "0.1.0"
edition = "2024"
license = "UNLICENSED"
description = "CLI for discovering PX docs decisions and guides"

[[bin]]
name = "pxdocs"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
